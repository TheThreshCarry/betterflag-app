# deps
node_modules/

# builds
.next/
dist/
.turbo/
.wrangler/
*.tsbuildinfo

# env & secrets
.env
.env.*
!.env.example
!*.env.example

# wrangler local dev secrets (per-worker); *.dev.vars.example templates commit
.dev.vars
!*.dev.vars.example

# test artifacts
coverage/
playwright-report/
test-results/

# supabase CLI local state (link/temp); real config lives in packages/db/supabase
**/supabase/.temp/
**/supabase/.branches/
/supabase/

# this repo uses bun, bun.lock is committed (CI runs `bun install --frozen-lockfile`).
# Don't commit a stray pnpm lockfile.
pnpm-lock.yaml

# misc
.DS_Store
