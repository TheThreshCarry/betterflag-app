{
  "name": "shipos",
  "private": true,
  "packageManager": "bun@1.2.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.10.3",
    "typescript": "^7.0.1-rc"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
