# Supabase (same project as the dashboard app)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Comma-separated Betterflag team emails allowed into this console
BETTERFLAG_ADMIN_EMAILS=

# Optional: evaluation analytics (charts degrade gracefully when unset)
CLICKHOUSE_URL=
CLICKHOUSE_USER=
CLICKHOUSE_PASSWORD=

# Optional: config KV cleanup on org delete (no-ops with a warning when unset)
CF_ACCOUNT_ID=
CF_API_TOKEN=
CF_KV_NAMESPACE_ID=
